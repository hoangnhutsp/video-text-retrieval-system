# Video-Text Retrieval System Requirements

# Core dependencies
torch>=1.9.0
torchvision>=0.10.0
transformers>=4.20.0

# Image processing
Pillow>=8.0.0
opencv-python>=4.5.0
numpy>=1.21.0

# Data processing
pandas>=1.3.0
tqdm>=4.60.0

# Network and file transfer
paramiko>=2.7.0  # For SFTP dataset download
python-dotenv>=0.19.0  # For .env file support

# OCR (Optical Character Recognition) - choose one
easyocr>=1.6.0  # Preferred OCR library
# pytesseract>=0.3.0  # Alternative OCR (uncomment if using instead of easyocr)

# Vector search
faiss-cpu>=1.7.0  # Use faiss-gpu if you have GPU with CUDA

# Note: Some packages are built into Python:
# - pathlib (built-in since Python 3.4)
# - dataclasses (built-in since Python 3.7)  
# - json (built-in)
# - pickle (built-in)
# - datetime (built-in)
# - logging (built-in)